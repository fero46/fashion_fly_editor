.editor.clear
  .editor__wrapper
    .editor__canvas{ 'droppable' => true }

  = render 'editor_pane'

  .editor__actions.clear{ ng: {controller: 'ActionsController'} }
    = link_to "Kollektion speichern", '#', class: 'btn btn-save', 'ng-click' => 'saveCollection()'
